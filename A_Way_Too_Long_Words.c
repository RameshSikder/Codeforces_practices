#include <stdio.h>
#include <string.h>

int main(){
    char s[101];
    scanf("%s",s);
    int l = strlen(s) - 1;
    if(l<=10){
        printf("%s\n",s);
    }
    else{
        char str[20];
        str[0]=str[0];
        str[1]=
        printf("%s\n",str);
    }
    return 0;
}